{
  "mcpServers": {
    "deeplearn": {
      "command": "node",
      "args": ["${CLAUDE_PLUGIN_ROOT}/server/dist/index.js"],
      "env": {
        "DEEPLEARN_VAULT_PATH": "${user_config.vault_path}"
      }
    }
  }
}
